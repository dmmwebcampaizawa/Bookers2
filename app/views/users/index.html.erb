<h1>ユーザー一覧</h1>

<h2>ユーザーの詳細</h2>
<div>
  <h3><%= @user.name %></h3>
  <%= image_tag @user.get_profile_image(100,100) %>
</div>

<h2>ユーザーの投稿一覧</h2> 

<% @user.each do |post_image| %>
<div>
  <%= link_to book_path(book.id) do %>
  <% end %>
  <p>image：<%= image_tag post_image.user.get_profile_image(100,100) %></p>
  <p>name：<%= post_image.shop_name %></p>
  <p>Introduction：<%= post_image.caption %></p>
</div>
<% end %>
