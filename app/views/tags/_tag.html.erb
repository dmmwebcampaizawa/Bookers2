<% if user_signed_in? %>
        <div class="search_form col-mb-3 px-5 mx-auto">
          <%= form_with model: @tag do |f| %>
          <%= f.text_field :tag_name %>
          <%= f.submit "検索", class: "btn btn-primary" %>
          <% end %>
        </div>
<% end %>